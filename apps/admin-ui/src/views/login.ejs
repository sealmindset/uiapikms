<!doctype html>
<html>
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title><%= title %></title>
    <style>
      body { font-family: Inter, system-ui, -apple-system, Segoe UI, Roboto, "Helvetica Neue", Arial; margin: 0; padding: 2rem; background:#0b1020; color:#e6e8ef; }
      .card { max-width: 420px; margin: 10vh auto; background:#121733; border:1px solid #2a2f4a; border-radius:12px; padding: 2rem; box-shadow: 0 10px 25px rgba(0,0,0,0.25); }
      a.btn { display:inline-block; background:#4f46e5; color:white; padding:.75rem 1rem; border-radius:8px; text-decoration:none; font-weight:600; }
      a.btn:hover { background:#4338ca; }
      .muted { color:#a3a7bf; }
    </style>
  </head>
  <body>
    <div class="card">
      <h1 style="margin-top:0">Admin Login</h1>
      <p class="muted">Development mock login for the <strong><%= app %></strong> app.</p>
      <p><a class="btn" href="/auth/mock">Continue with Mock</a></p>
    </div>
  </body>
</html>
